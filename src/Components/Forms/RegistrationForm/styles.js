import { StyleSheet } from 'react-native'
export default styles = StyleSheet.create({
    container: {
      flex: 1,
    },
    fieldsContainer:{
      flex: 4,
      justifyContent: 'center',
      alignContent: 'center',
      alignItems: 'center',
    },
    buttonContainer:{
      marginTop:5*vw,
      flex:1,
      justifyContent: 'center',
      alignContent: 'center',
      alignItems: 'center',
    }
  });
  